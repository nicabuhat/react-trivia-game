import React from 'react';

export default function CategorySelector() {
    return (
        <div className="category-selector">
            <p>Select Category</p>
            <select></select>
        </div>
    );
}
